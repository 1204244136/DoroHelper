name: Compile AHK 2.0 and Release

on:
  push:
    tags:
      - "v**"
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: windows-latest # AutoHotkey 和 Ahk2Exe 在 Windows 上运行
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install AutoHotkey v2 and Setup Ahk2Exe
        shell: pwsh
        run: |
          Write-Host "Attempting to uninstall potentially conflicting AutoHotkey versions to ensure a clean slate..."
          choco uninstall autohotkey.install -y --remove-dependencies -nfy || Write-Warning "autohotkey.install not found or uninstall failed, continuing..."
          choco uninstall autohotkey -y --remove-dependencies -nfy         || Write-Warning "autohotkey (meta) not found or uninstall failed, continuing..."
          choco uninstall autohotkey.v1 -y --remove-dependencies -nfy      || Write-Warning "autohotkey.v1 not found or uninstall failed, continuing..."
          # -nfy: --no-progress, --force, --yes (combines common flags)

          $ahkInstallDirRoot = "$env:ProgramFiles\AutoHotkey"
          if (Test-Path $ahkInstallDirRoot) {
            Write-Host "Removing existing AutoHotkey installation directory: $ahkInstallDirRoot to ensure a clean install."
            Remove-Item -Recurse -Force $ahkInstallDirRoot -ErrorAction SilentlyContinue
            Start-Sleep -Seconds 3 # Allow time for file system operations
          }

          Write-Host "Installing AutoHotkey.v2 via Chocolatey..."
          choco install autohotkey.v2 -y --force --no-progress

          $ahkInstallDir = "$env:ProgramFiles\AutoHotkey" # Standard install location for the choco package
          Write-Host "AutoHotkey v2 nominal installation directory: $ahkInstallDir"

          if (-not (Test-Path $ahkInstallDir)) {
            Write-Error "AutoHotkey v2 installation directory '$ahkInstallDir' not found after choco install. This is unexpected."
            exit 1
          }

          Write-Host "Listing all EXE files in AutoHotkey installation directory '$ahkInstallDir' with version info:"
          Get-ChildItem -Path $ahkInstallDir -Filter "*.exe" | ForEach-Object {
            $versionInfo = "N/A"
            try { $versionInfo = $_.VersionInfo.ProductVersion } catch {}
            [PSCustomObject]@{
                Name = $_.Name
                Length = $_.Length
                ProductVersion = $versionInfo
                FullName = $_.FullName
            }
          } | Format-Table -AutoSize

          # Find a suitable AutoHotkey v2 executable, prioritizing common v2 names
          $ahkExePathToUse = ""
          # Order of preference for v2 executables
          $v2ExeNames = @("AutoHotkey64.exe", "AutoHotkeyU64.exe", "AutoHotkey.exe") 

          foreach ($exeName in $v2ExeNames) {
              $currentTestPath = Join-Path $ahkInstallDir $exeName
              if (Test-Path $currentTestPath) {
                  Write-Host "Checking version of '$currentTestPath'..."
                  $fileProductVersion = ""
                  try {
                      $fileProductVersion = (Get-Item $currentTestPath).VersionInfo.ProductVersion
                  } catch {
                      Write-Warning "Could not retrieve ProductVersion for '$currentTestPath'."
                  }

                  Write-Host "ProductVersion for '$currentTestPath' is '$fileProductVersion'."
                  # AutoHotkey v2.x versions usually start with "2."
                  if ($fileProductVersion -and $fileProductVersion.StartsWith("2.")) {
                      Write-Host "'$currentTestPath' identified as AutoHotkey v2 (ProductVersion: $fileProductVersion)."
                      $ahkExePathToUse = $currentTestPath
                      break 
                  } else {
                      Write-Warning "'$currentTestPath' (ProductVersion: $fileProductVersion) does not appear to be a v2 executable or version unknown. Checking next candidate."
                  }
              }
          }

          if (-not $ahkExePathToUse) {
              Write-Error "Could not find a confirmed AutoHotkey v2 executable in '$ahkInstallDir' (tried $($v2ExeNames -join ', ')). Please check the EXE listing above."
              exit 1
          }
          Write-Host "Using AutoHotkey v2 executable: '$ahkExePathToUse'"

          # Add AutoHotkey main directory to PATH using the directory of the chosen executable
          $chosenAhkDir = Split-Path $ahkExePathToUse -Parent
          Write-Host "Adding AutoHotkey core directory ('$chosenAhkDir') to PATH for this session."
          $env:PATH = "$chosenAhkDir;" + $env:PATH

          # Run install-ahk2exe.ahk script
          $installScriptDir = Join-Path $ahkInstallDir "UX" # UX is typically relative to the root install dir
          $installScriptName = "install-ahk2exe.ahk"
          $installScriptFullPath = Join-Path $installScriptDir $installScriptName
          $ahk2ExeCompilerDir = Join-Path $ahkInstallDir "Compiler"
          $ahk2ExeFinalPath = Join-Path $ahk2ExeCompilerDir "Ahk2Exe.exe"

          if (Test-Path $installScriptFullPath) {
            Write-Host "Found '$installScriptName'. Running it with '$ahkExePathToUse' to ensure Ahk2Exe is installed..."
            Push-Location $installScriptDir
            & $ahkExePathToUse /ErrorStdOut $installScriptName # Run the v2 script with the v2 executable
            $exitCode = $LASTEXITCODE
            Pop-Location
            
            if ($exitCode -ne 0) {
                Write-Error "'$installScriptName' execution failed with exit code $exitCode. Check logs for errors from the script itself (stdout/stderr)."
            } else {
                Write-Host "'$installScriptName' executed successfully (exit code $exitCode)."
            }

            Write-Host "Waiting a few seconds for Ahk2Exe installation/download to complete..."
            Start-Sleep -Seconds 20 # Allow time for download and extraction

            if (Test-Path $ahk2ExeFinalPath) {
              Write-Host "Ahk2Exe.exe confirmed/installed at '$ahk2ExeFinalPath'."
            } else {
              Write-Warning "Ahk2Exe.exe NOT found at '$ahk2ExeFinalPath' even after running '$installScriptName'."
              Write-Host "Listing contents of Compiler directory '$ahk2ExeCompilerDir' (if it exists):"
              if (Test-Path $ahk2ExeCompilerDir) { Get-ChildItem -Path $ahk2ExeCompilerDir } else { Write-Host "Directory does not exist."}
            }
          } else {
            Write-Warning "'$installScriptFullPath' not found. Ahk2Exe might not be set up by this method."
            Write-Host "Listing contents of UX directory '$installScriptDir' (if it exists):"
            if (Test-Path $installScriptDir) { Get-ChildItem -Path $installScriptDir } else { Write-Host "Directory does not exist."}
          }

          # Add Compiler directory to PATH if it exists
          if (Test-Path $ahk2ExeCompilerDir) {
            Write-Host "Adding AutoHotkey Compiler directory to PATH for this session: $ahk2ExeCompilerDir"
            $env:PATH = "$ahk2ExeCompilerDir;" + $env:PATH
          } else {
            Write-Warning "AutoHotkey Compiler directory '$ahk2ExeCompilerDir' does not exist. Ahk2Exe will likely not be found in PATH."
          }

          Write-Host "Final session PATH: $env:PATH"
          Start-Sleep -Milliseconds 500

          # Verify AutoHotkey command (the one we chose, by its name)
          $chosenAhkExeName = Split-Path $ahkExePathToUse -Leaf
          Write-Host "Verifying selected AutoHotkey command '$chosenAhkExeName' is in PATH..."
          if (-not (Get-Command $chosenAhkExeName -ErrorAction SilentlyContinue)) {
            Write-Error "$chosenAhkExeName not found using Get-Command after PATH adjustment. Current PATH: $env:PATH"
            exit 1
          }
          Write-Host "$chosenAhkExeName found via Get-Command at: $((Get-Command $chosenAhkExeName).Source)"

          # Verify Ahk2Exe.exe
          Write-Host "Verifying Ahk2Exe command is in PATH..."
          if (-not (Get-Command Ahk2Exe -ErrorAction SilentlyContinue)) {
            Write-Error "Ahk2Exe.exe not found using Get-Command after PATH adjustment. Current PATH: $env:PATH"
            Write-Host "Checking expected location: $ahk2ExeFinalPath (Exists: $(Test-Path $ahk2ExeFinalPath))"
            exit 1
          }
          Write-Host "Ahk2Exe found via Get-Command at: $((Get-Command Ahk2Exe).Source)"

      - name: Compile AutoHotkey Script
        id: compile_script
        shell: pwsh
        run: |
          $ahkExtractPath = "AutoHotkey_v2_Files" # 与上面解压路径一致
          $scriptFile = "DoroHelper.ahk"
          $iconFile = "doro.ico"
          $outputExe = "DoroHelper.exe" # 输出的 exe 文件名

          if (-not (Test-Path $scriptFile)) {
            Write-Error "AHK script file '$scriptFile' not found."
            exit 1
          }
          if (-not (Test-Path $iconFile)) {
            Write-Warning "Icon file '$iconFile' not found. Proceeding without custom icon."
            $iconFile = "" # 如果图标不存在，则不使用图标参数
          }

          # 确定用于编译的64位基础二进制文件路径
          # 根据 AutoHotkey v2.0.19.zip 的内容，通常是 AutoHotkey64.exe 或 AutoHotkeyU64.exe
          # 在 AutoHotkey_v2_Files 或 AutoHotkey_v2_Files\UX 目录下
          $baseBinaryName = "AutoHotkey64.exe"
          $baseBinaryPath = Join-Path $PWD $ahkExtractPath $baseBinaryName
          if (-not (Test-Path $baseBinaryPath)) {
              $baseBinaryPath = Join-Path $PWD $ahkExtractPath "UX" $baseBinaryName # 检查 UX 子目录
              if (-not (Test-Path $baseBinaryPath)) {
                  # 如果 AutoHotkey64.exe 未找到，尝试 AutoHotkeyU64.exe
                  $baseBinaryName = "AutoHotkeyU64.exe"
                  $baseBinaryPath = Join-Path $PWD $ahkExtractPath $baseBinaryName
                   if (-not (Test-Path $baseBinaryPath)) {
                        $baseBinaryPath = Join-Path $PWD $ahkExtractPath "UX" $baseBinaryName # 检查 UX 子目录
                        if (-not (Test-Path $baseBinaryPath)) {
                            Write-Error "Could not find a suitable 64-bit base binary (AutoHotkey64.exe or AutoHotkeyU64.exe) in '$ahkExtractPath' or '$($ahkExtractPath)\UX'."
                            Write-Host "Listing contents of ${ahkExtractPath}:"
                            Get-ChildItem -Path (Join-Path $PWD $ahkExtractPath) -Recurse | Select-Object FullName
                            exit 1
                        }
                   }
              }
          }

          Write-Host "Using Ahk2Exe from PATH."
          Write-Host "Script: $scriptFile"
          Write-Host "Output: $outputExe"
          Write-Host "Icon: $iconFile"
          Write-Host "Base Binary for compilation: $baseBinaryPath"

          $compileArgs = @("/in", "`"$scriptFile`"", "/out", "`"$outputExe`"", "/bin", "`"$baseBinaryPath`"", "/cp", "65001") # UTF-8 codepage
          if ($iconFile -and (Test-Path $iconFile)) {
              $compileArgs += @("/icon", "`"$iconFile`"")
          }

          Ahk2Exe.exe $compileArgs

          if (-not (Test-Path $outputExe)) {
            Write-Error "Compilation failed. $outputExe not found."
            exit 1
          }
          Write-Host "$outputExe compiled successfully."
          echo "EXE_NAME=$outputExe" >> $env:GITHUB_OUTPUT

      - name: Determine if Pre-release
        id: prerelease_check
        shell: bash # bash 更易于进行字符串检查
        run: |
          VERSION_TAG="${{ steps.get_version.outputs.VERSION_TAG }}"
          IS_PRERELEASE="false"
          if [[ "$VERSION_TAG" == *beta* ]]; then
            IS_PRERELEASE="true"
          fi
          echo "IS_PRERELEASE=$IS_PRERELEASE" >> $GITHUB_OUTPUT
          echo "Release version: $VERSION_TAG, Is Prerelease: $IS_PRERELEASE"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_version.outputs.VERSION_TAG }}
          name: ${{ steps.get_version.outputs.VERSION_TAG }} # Release 标题
          body: |
            Automated release for DoroHelper ${{ steps.get_version.outputs.VERSION_TAG }}
            Compiled from commit ${{ github.sha }}
          draft: false
          prerelease: ${{ steps.prerelease_check.outputs.IS_PRERELEASE }}
          files: |
            ${{ steps.compile_script.outputs.EXE_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
